# Copy this file to `.env` when running `ai_linebot_core` standalone.
OPENAI_API_KEY=your_openai_api_key

# Optional: override the model used for LLM-based scenario judgment.
OPENAI_MODEL=gpt-4.1-mini
